// =================== CHANGING COLOR OF POPUP MENU BUTTON===============
// In order to change the color of popup menu button, You can override the
// default icon by adding a new icon property in it and specifying a new color.
//===============================================================================
//--------------------------------------------------------------------------------
// when connection state is waiting means we are waiting for stream to contain
// first value, as soon as it returns first value, after that the connection
// state will be active.
// falling through cases in switch, when the first case has no
// logic so it falls through the next case, this case will provide logic for
// both of them.
//=================================================================================
//--------------------------------------------------------------------------------
// for using some function that returns future in your UI, you can use
// the future builder for this.
//--------------------------------------------------------------------------------
//Everytime you do hot reload, it calls the builder function of UI, so if the
// builder function is performing future that create new note, so everytime
// hot reload, a new note will be created.
// How to handle such situation?
// for that we need to keep hold of our current note, so it is not recreated
// everytime the build function is called.
//=============================================================================
// using text editing controller sync data whatever using is typing to the firebase
//--------------------------------------------------------------------------------
// if the view is disposed off, or like back button is pressed so
// current note if it is empty should be deleted.
// if we dont do this then the view will be full of empty notes,
// everytime move from screen
//=====================================================================
// when move away current screen to another screen ,screen is removed
// from the widget tree calling the dispose() method
//=====================================================================
// in modern note taking app's there is no save button, so all
// content is automatically saved.
//---------------------------------------------------------------------
// initState is a critical part of the StatefulWidget lifecycle
// for initializing the widget's state, setting up controllers and
// listeners, and performing any one-time setups that your widget
// needs before it is rendered for the first time.
//=======================================================================================
// snapshot gives latest updates about the future like it's data, connectionState.
